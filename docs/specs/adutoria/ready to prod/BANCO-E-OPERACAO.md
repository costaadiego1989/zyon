# Banco, migrações e operação

## Migrações atuais

[apps/api/prisma.config.ts:3](<../../../../apps/api/prisma.config.ts#L3>) seleciona prisma/deploy-migrations. A baseline ativa contém CREATE TABLE para os **118 modelos** do schema. As 51 migrações em prisma/migrations são históricas e não foram confundidas com o caminho de deploy. [apps/api/prisma/DEPLOYMENT.md:1](<../../../../apps/api/prisma/DEPLOYMENT.md#L1>) documenta adoção distinta para banco novo e existente.

Status: **REVIEWED_STATIC; INFRA VALIDATION REQUIRED**. Não foram executados migrate deploy/resolve/reset nem alterações em banco. A existência do DDL não prova que produção tenha a mesma estrutura, constraints ou índices. Adoção em banco existente requer comparação e backup/restauração comprovados.

## Índices e integridade

Há unique de payment idempotency, IDs externos, merchant settings e offer aplicada. O estoque tem unique variante/warehouse, mas reserva não carrega warehouse/stockId e não possui unique de cart+variant; consultar API-002. Índices simples de status/data não resolvem atomicidade de estado.

| Modelo / tabela | Tenant/campos associados | Índices/constraints no schema | DDL ativo |
| --- | --- | --- | --- |
| [apps/api/prisma/schema.prisma:16](<../../../../apps/api/prisma/schema.prisma#L16>)<br>Merchant / merchants | Sem campo merchantId detectado; ownership pode vir por relação | id                      String       @id | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:66](<../../../../apps/api/prisma/schema.prisma#L66>)<br>MerchantOnboardingState / merchant_onboarding_states | merchantId String   @id @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade) | merchantId String   @id @map("merchant_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:77](<../../../../apps/api/prisma/schema.prisma#L77>)<br>MerchantInstallation / merchant_installations | merchantId     String    @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@unique([merchantId, name, environment]); @@index([merchantId, environment, status]) | id             String    @id @default(cuid()); @@unique([merchantId, name, environment]); @@index([merchantId, environment, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:98](<../../../../apps/api/prisma/schema.prisma#L98>)<br>MerchantAuditEvent / merchant_audit_events | merchantId    String   @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, occurredAt, id]); @@index([merchantId, resourceType, resourceId]) | id            String   @id @default(cuid()); @@index([merchantId, occurredAt, id]); @@index([merchantId, resourceType, resourceId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:120](<../../../../apps/api/prisma/schema.prisma#L120>)<br>MerchantCommerceConnection / merchant_commerce_connections | merchantId       String    @id @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade) | merchantId       String    @id @map("merchant_id"); @@index([provider, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:139](<../../../../apps/api/prisma/schema.prisma#L139>)<br>MerchantPaymentConnection / merchant_payment_connections | merchantId        String    @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@id([merchantId, provider]) | @@id([merchantId, provider]); @@index([provider, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:162](<../../../../apps/api/prisma/schema.prisma#L162>)<br>MerchantBillingSubscription / merchant_billing_subscriptions | merchantId           String    @id @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade) | merchantId           String    @id @map("merchant_id"); stripeCustomerId     String?   @unique @map("stripe_customer_id"); stripeSubscriptionId String?   @unique @map("stripe_subscription_id"); @@index([status, trialEndsAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:180](<../../../../apps/api/prisma/schema.prisma#L180>)<br>CommercePendingOrder / commerce_pending_orders | merchantId       String   @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@id([merchantId, sessionId]); @@index([merchantId, status, updatedAt]) | @@id([merchantId, sessionId]); @@index([merchantId, status, updatedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:196](<../../../../apps/api/prisma/schema.prisma#L196>)<br>CommercePaidEvent / commerce_paid_events | merchantId       String   @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@id([merchantId, paymentReference]); @@index([merchantId, createdAt]) | @@id([merchantId, paymentReference]); @@index([merchantId, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:209](<../../../../apps/api/prisma/schema.prisma#L209>)<br>MerchantUser / merchant_users | merchantId      String   @map("merchant_id"); merchant    Merchant             @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId]) | id              String   @id @default(cuid()); @@unique([email]); @@index([merchantId]); @@index([oauthProvider, oauthProviderId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:230](<../../../../apps/api/prisma/schema.prisma#L230>)<br>AgentRule / agent_rules | merchantId       String   @map("merchant_id"); @@unique([merchantId, agentId]); @@unique([merchantId, userId, scope]); @@index([merchantId, userId]) | id               String   @id @default(cuid()); @@unique([merchantId, agentId]); @@unique([merchantId, userId, scope]); @@index([merchantId, userId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:251](<../../../../apps/api/prisma/schema.prisma#L251>)<br>CheckoutSetting / checkout_settings | merchantId         String   @unique @map("merchant_id"); @@index([merchantId]) | id                 String   @id @default(cuid()); merchantId         String   @unique @map("merchant_id"); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:268](<../../../../apps/api/prisma/schema.prisma#L268>)<br>CheckoutSession / checkout_sessions | merchantId       String   @map("merchant_id"); @@unique([merchantId, sessionId]); @@index([merchantId, updatedAt]); @@index([merchantId, createdAt]); @@index([globalUserId, merchantId, createdAt]) | id               String   @id @default(cuid()); @@unique([merchantId, sessionId]); @@index([merchantId, updatedAt]); @@index([merchantId, createdAt]); @@index([globalUserId, merchantId, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:298](<../../../../apps/api/prisma/schema.prisma#L298>)<br>CheckoutEvent / checkout_events | merchantId String   @map("merchant_id"); session CheckoutSession @relation(fields: [merchantId, sessionId], references: [merchantId, sessionId], onDelete: Cascade); @@index([merchantId, sessionId, occurredAt]) | id         String   @id @default(cuid()); @@index([merchantId, sessionId, occurredAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:311](<../../../../apps/api/prisma/schema.prisma#L311>)<br>BuyerIdentity / buyer_identities | merchantId   String @map("merchant_id"); @@unique([merchantId, identityKey]); @@index([merchantId, globalUserId]) | id           String @id @default(cuid()); @@unique([merchantId, identityKey]); @@index([merchantId, globalUserId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:322](<../../../../apps/api/prisma/schema.prisma#L322>)<br>MerchantRule / merchant_rules | merchantId                        String   @unique @map("merchant_id") | id                                String   @id @default(cuid()); merchantId                        String   @unique @map("merchant_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:346](<../../../../apps/api/prisma/schema.prisma#L346>)<br>SupportSetting / support_settings | merchantId String   @unique @map("merchant_id"); @@index([merchantId]) | id         String   @id @default(cuid()); merchantId String   @unique @map("merchant_id"); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:357](<../../../../apps/api/prisma/schema.prisma#L357>)<br>SupportTicket / support_tickets | merchantId   String    @map("merchant_id"); @@index([merchantId, status, createdAt]); @@index([merchantId, sessionId]) | id           String    @id; @@index([merchantId, status, createdAt]); @@index([merchantId, sessionId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:375](<../../../../apps/api/prisma/schema.prisma#L375>)<br>SupportTicketMessage / support_ticket_messages | Sem campo merchantId detectado; ownership pode vir por relação | id         String   @id @default(cuid()); @@index([ticketId, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:388](<../../../../apps/api/prisma/schema.prisma#L388>)<br>AuthorizedOffer / authorized_offers | merchantId       String   @map("merchant_id"); session        CheckoutSession @relation(fields: [merchantId, sessionId], references: [merchantId, sessionId], onDelete: Cascade); @@index([merchantId, sessionId]) | id               String   @id; @@index([merchantId, sessionId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:407](<../../../../apps/api/prisma/schema.prisma#L407>)<br>AcceptedOffer / accepted_offers | merchantId       String   @map("merchant_id"); session CheckoutSession @relation(fields: [merchantId, sessionId], references: [merchantId, sessionId], onDelete: Cascade); @@unique([merchantId, sessionId, offerId]); @@index([merchantId, acceptedAt]) | id               String   @id @default(cuid()); @@unique([merchantId, sessionId, offerId]); @@index([merchantId, acceptedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:426](<../../../../apps/api/prisma/schema.prisma#L426>)<br>CompletedOrder / completed_orders | merchantId         String    @map("merchant_id"); session CheckoutSession @relation(fields: [merchantId, sessionId], references: [merchantId, sessionId], onDelete: Cascade); @@unique([merchantId, sessionId, externalOrderId]); @@index([merchantId, completedAt]); @@index([merchantId, status, completedAt]) | id                 String    @id @default(cuid()); @@unique([merchantId, sessionId, externalOrderId]); @@index([merchantId, completedAt]); @@index([merchantId, status, completedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:448](<../../../../apps/api/prisma/schema.prisma#L448>)<br>MerchantApiKey / merchant_api_keys | merchantId    String    @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, environment, revokedAt]); @@index([merchantId, expiresAt]) | id            String    @id; keyHash       String    @unique @map("key_hash"); @@index([merchantId, environment, revokedAt]); @@index([merchantId, expiresAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:471](<../../../../apps/api/prisma/schema.prisma#L471>)<br>HttpIdempotencyRecord / http_idempotency_records | merchantId         String   @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@unique([merchantId, idempotencyKey]); @@index([merchantId, state, updatedAt]) | id                 String   @id @default(cuid()); @@unique([merchantId, idempotencyKey]); @@index([merchantId, state, updatedAt]); @@index([expiresAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:494](<../../../../apps/api/prisma/schema.prisma#L494>)<br>MerchantWebhookEndpoint / merchant_webhook_endpoints | merchantId    String   @map("merchant_id"); merchant   Merchant                  @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, enabled]) | id            String   @id; @@index([merchantId, enabled]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:512](<../../../../apps/api/prisma/schema.prisma#L512>)<br>MerchantWebhookDelivery / merchant_webhook_deliveries | merchantId     String    @map("merchant_id"); merchant Merchant                @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, status, nextAttemptAt]); @@index([merchantId, createdAt]) | id             String    @id; @@unique([endpointId, eventId]); @@index([merchantId, status, nextAttemptAt]); @@index([merchantId, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:540](<../../../../apps/api/prisma/schema.prisma#L540>)<br>Shipment / shipments | merchantId      String    @map("merchant_id"); merchant       Merchant        @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@unique([merchantId, externalOrderId]); @@unique([merchantId, trackingCode]); @@index([merchantId, status, updatedAt]) | id              String    @id; @@unique([merchantId, externalOrderId]); @@unique([merchantId, trackingCode]); @@index([merchantId, status, updatedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:563](<../../../../apps/api/prisma/schema.prisma#L563>)<br>TrackingEvent / tracking_events | merchantId   String   @map("merchant_id"); @@index([merchantId, trackingCode, occurredAt]) | id           String   @id; @@index([merchantId, trackingCode, occurredAt]); @@index([shipmentId, occurredAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:582](<../../../../apps/api/prisma/schema.prisma#L582>)<br>BuyerPurchaseRecord / buyer_purchase_records | merchantId         String   @map("merchant_id"); @@unique([merchantId, orderId]); @@index([merchantId, globalUserId]); @@index([merchantId, merchantCustomerId]); @@index([merchantId, completedAt]) | id                 String   @id @default(cuid()); @@unique([merchantId, orderId]); @@index([merchantId, globalUserId]); @@index([merchantId, merchantCustomerId]); @@index([merchantId, completedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:602](<../../../../apps/api/prisma/schema.prisma#L602>)<br>MerchantNegotiationPolicy / merchant_negotiation_policies | merchantId String   @id @map("merchant_id") | merchantId String   @id @map("merchant_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:611](<../../../../apps/api/prisma/schema.prisma#L611>)<br>BuyerAgentNegotiationPreference / buyer_agent_negotiation_preferences | merchantId   String   @map("merchant_id"); @@unique([merchantId, globalUserId]); @@index([merchantId]) | id           String   @id @default(cuid()); @@unique([merchantId, globalUserId]); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:624](<../../../../apps/api/prisma/schema.prisma#L624>)<br>NegotiationSession / negotiation_sessions | merchantId           String   @map("merchant_id"); @@index([merchantId, createdAt]) | id                   String   @id @default(cuid()); @@index([merchantId, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:640](<../../../../apps/api/prisma/schema.prisma#L640>)<br>NegotiationCostLedgerEntry / negotiation_cost_ledger_entries | merchantId           String   @map("merchant_id"); @@index([merchantId, negotiationSessionId]) | id                   String   @id @default(cuid()); @@unique([negotiationSessionId, eventType]) // H2 fix: concurrent-apply protection; @@index([merchantId, negotiationSessionId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:657](<../../../../apps/api/prisma/schema.prisma#L657>)<br>BuyerAgent / buyer_agents | merchantId     String    @map("merchant_id"); @@unique([merchantId, globalUserId]); @@index([merchantId, status]) | id             String    @id @default(cuid()); @@unique([merchantId, globalUserId]); @@index([merchantId, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:676](<../../../../apps/api/prisma/schema.prisma#L676>)<br>AgentReputation / agent_reputations | Sem campo merchantId detectado; ownership pode vir por relação | id               String   @id @default(cuid()); agentId          String   @unique @map("agent_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:689](<../../../../apps/api/prisma/schema.prisma#L689>)<br>M2MProtocolConfig / m2m_protocol_configs | merchantId           String   @unique @map("merchant_id") | id                   String   @id @default(cuid()); merchantId           String   @unique @map("merchant_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:702](<../../../../apps/api/prisma/schema.prisma#L702>)<br>OutboxMessage / outbox_messages | merchantId    String    @map("merchant_id"); @@index([merchantId, createdAt]) | eventId       String    @id @map("event_id"); @@index([merchantId, createdAt]); @@index([status, createdAt]); @@index([status, nextAttemptAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:726](<../../../../apps/api/prisma/schema.prisma#L726>)<br>OutboxHandlerExecution / outbox_handler_executions | Sem campo merchantId detectado; ownership pode vir por relação | @@id([eventId, handlerId]); @@index([eventId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:736](<../../../../apps/api/prisma/schema.prisma#L736>)<br>CheckoutIntervention / checkout_interventions | merchantId String   @map("merchant_id"); @@index([merchantId, sessionId, occurredAt]) | id         String   @id @default(cuid()); @@index([merchantId, sessionId, occurredAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:747](<../../../../apps/api/prisma/schema.prisma#L747>)<br>ShippingQuote / shipping_quotes | merchantId         String   @map("merchant_id"); @@index([merchantId, sessionId, createdAt]); @@index([merchantId, quoteKey, expiresAt]) | id                 String   @id; @@index([merchantId, sessionId, createdAt]); @@index([merchantId, quoteKey, expiresAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:763](<../../../../apps/api/prisma/schema.prisma#L763>)<br>PaymentIntent / payment_intents | merchantId          String   @map("merchant_id"); @@unique([merchantId, sessionId, idempotencyKey]); @@unique([merchantId, providerPaymentId]); @@index([merchantId, sessionId]); @@index([merchantId, commerceOrderId]) | id                  String   @id; @@unique([merchantId, sessionId, idempotencyKey]); @@unique([merchantId, providerPaymentId]); @@index([merchantId, sessionId]); @@index([merchantId, commerceOrderId]); @@index([status, updatedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:789](<../../../../apps/api/prisma/schema.prisma#L789>)<br>PaymentProviderEvent / payment_provider_events | merchantId  String?  @map("merchant_id"); @@unique([provider, merchantId, eventId]); @@index([merchantId, processedAt]) | id          String   @id; @@unique([provider, merchantId, eventId]); @@index([merchantId, processedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:801](<../../../../apps/api/prisma/schema.prisma#L801>)<br>PaymentCryptoTransfer / payment_crypto_transfers | merchantId String   @map("merchant_id"); @@index([merchantId, intentId]) | id         String   @id; @@unique([chain, txHash]); @@index([merchantId, intentId]); @@index([expiresAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:816](<../../../../apps/api/prisma/schema.prisma#L816>)<br>BuyerAccount / buyer_accounts | Sem campo merchantId detectado; ownership pode vir por relação | globalUserId String   @id @default(cuid()) @map("global_user_id"); email        String   @unique | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:839](<../../../../apps/api/prisma/schema.prisma#L839>)<br>BuyerAddress / buyer_addresses | Sem campo merchantId detectado; ownership pode vir por relação | id           String   @id @default(cuid()); @@index([globalUserId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:862](<../../../../apps/api/prisma/schema.prisma#L862>)<br>BuyerConversation / buyer_conversations | merchantId    String   @map("merchant_id"); @@index([merchantId, sessionId]) | id            String   @id @default(cuid()); @@index([globalUserId, lastMessageAt]); @@index([merchantId, sessionId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:880](<../../../../apps/api/prisma/schema.prisma#L880>)<br>BuyerPreference / buyer_preferences | Sem campo merchantId detectado; ownership pode vir por relação | id                       String   @id @default(cuid()); globalUserId             String   @unique @map("global_user_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:897](<../../../../apps/api/prisma/schema.prisma#L897>)<br>BuyerAgentProfile / buyer_agent_profiles | Sem campo merchantId detectado; ownership pode vir por relação | id                               String    @id @default(cuid()); globalUserId                     String    @unique @map("global_user_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:923](<../../../../apps/api/prisma/schema.prisma#L923>)<br>BuyerPhoneOtp / buyer_phone_otps | Sem campo merchantId detectado; ownership pode vir por relação | id          String    @id @default(cuid()); phone       String    @unique; @@index([expiresAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:943](<../../../../apps/api/prisma/schema.prisma#L943>)<br>WebAuthnCredential / webauthn_credentials | Sem campo merchantId detectado; ownership pode vir por relação | id           String    @id @default(cuid()); credentialId String    @unique @map("credential_id"); @@index([globalUserId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:965](<../../../../apps/api/prisma/schema.prisma#L965>)<br>LoginAttemptCounter / login_attempt_counters | Sem campo merchantId detectado; ownership pode vir por relação | id           String   @id @default(cuid()); scopeKey     String   @unique @map("scope_key"); @@index([windowEndsAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:978](<../../../../apps/api/prisma/schema.prisma#L978>)<br>Coupon / coupons | merchantId               String    @map("merchant_id"); @@unique([merchantId, code]); @@index([merchantId, status]) | id                       String    @id; @@unique([merchantId, code]); @@index([merchantId, status]); @@index([endsAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1008](<../../../../apps/api/prisma/schema.prisma#L1008>)<br>CouponRedemption / coupon_redemptions | merchantId        String   @map("merchant_id"); @@unique([merchantId, sessionId, couponId]); @@index([merchantId, couponId, status]); @@index([merchantId, buyerGlobalUserId, couponId, status]) | id                String   @id; @@unique([merchantId, sessionId, couponId]); @@index([merchantId, couponId, status]); @@index([merchantId, buyerGlobalUserId, couponId, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1033](<../../../../apps/api/prisma/schema.prisma#L1033>)<br>CrossSellPromotion / cross_sell_promotions | merchantId         String    @map("merchant_id"); @@index([merchantId, status]) | id                 String    @id; @@index([merchantId, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1053](<../../../../apps/api/prisma/schema.prisma#L1053>)<br>CrossSellSuggestion / cross_sell_suggestions | merchantId       String    @map("merchant_id"); @@index([merchantId, sessionId, promoId, status]); @@index([merchantId, status]) | id               String    @id; @@index([merchantId, sessionId, promoId, status]); @@index([merchantId, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1075](<../../../../apps/api/prisma/schema.prisma#L1075>)<br>PriceQuoteJob / price_quote_jobs | merchantId        String    @map("merchant_id"); @@index([merchantId, status, createdAt]); @@index([merchantId, sessionId]) | id                String    @id; @@index([merchantId, status, createdAt]); @@index([merchantId, sessionId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1098](<../../../../apps/api/prisma/schema.prisma#L1098>)<br>SelfCheckoutBuyerUser / self_checkout_buyer_users | merchantId       String   @map("merchant_id"); @@unique([merchantId, email]) | id               String   @id; @@unique([merchantId, email]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1117](<../../../../apps/api/prisma/schema.prisma#L1117>)<br>SelfCheckoutWallet / self_checkout_wallets | Sem campo merchantId detectado; ownership pode vir por relação | id          String   @id; buyerUserId String   @unique @map("buyer_user_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1133](<../../../../apps/api/prisma/schema.prisma#L1133>)<br>SelfCheckoutSavedAddress / self_checkout_saved_addresses | Sem campo merchantId detectado; ownership pode vir por relação | id        String  @id; @@index([walletId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1150](<../../../../apps/api/prisma/schema.prisma#L1150>)<br>SelfCheckoutSavedPaymentMethod / self_checkout_saved_payment_methods | Sem campo merchantId detectado; ownership pode vir por relação | id           String   @id; @@index([walletId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1167](<../../../../apps/api/prisma/schema.prisma#L1167>)<br>SelfCheckoutTemplate / self_checkout_templates | merchantId                String   @map("merchant_id"); @@index([merchantId, buyerUserId]) | id                        String   @id; @@index([buyerUserId]); @@index([merchantId, buyerUserId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1201](<../../../../apps/api/prisma/schema.prisma#L1201>)<br>ProductCategory / product_categories | merchantId  String   @map("merchant_id"); @@unique([merchantId, slug]); @@index([merchantId]) | id          String   @id @default(cuid()); @@unique([merchantId, slug]); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1223](<../../../../apps/api/prisma/schema.prisma#L1223>)<br>Product / products | merchantId      String    @map("merchant_id"); @@index([merchantId]); @@index([merchantId, isActive, createdAt]); @@index([merchantId, categoryId, isActive]) | id              String    @id @default(cuid()); slug            String?   @unique; @@index([merchantId]); @@index([merchantId, isActive, createdAt]); @@index([merchantId, categoryId, isActive]); @@index([slug]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1255](<../../../../apps/api/prisma/schema.prisma#L1255>)<br>ProductVariant / product_variants | Sem campo merchantId detectado; ownership pode vir por relação | id          String   @id @default(cuid()); @@unique([productId, sku]); @@index([productId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1280](<../../../../apps/api/prisma/schema.prisma#L1280>)<br>ProductMedia / product_media | Sem campo merchantId detectado; ownership pode vir por relação | id        String    @id @default(cuid()); @@index([variantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1295](<../../../../apps/api/prisma/schema.prisma#L1295>)<br>ProductPrice / product_prices | Sem campo merchantId detectado; ownership pode vir por relação | id               String   @id @default(cuid()); variantId        String   @unique @map("variant_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1310](<../../../../apps/api/prisma/schema.prisma#L1310>)<br>ProductStock / product_stock | Sem campo merchantId detectado; ownership pode vir por relação | id          String   @id @default(cuid()); @@unique([variantId, warehouseId]); @@index([variantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1326](<../../../../apps/api/prisma/schema.prisma#L1326>)<br>StockReservation / stock_reservations | Sem campo merchantId detectado; ownership pode vir por relação | id        String                 @id @default(cuid()); @@index([variantId, status]); @@index([expiresAt, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1342](<../../../../apps/api/prisma/schema.prisma#L1342>)<br>ProductReview / product_reviews | Sem campo merchantId detectado; ownership pode vir por relação | id        String   @id @default(cuid()); @@index([productId, approved]); @@index([buyerId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1359](<../../../../apps/api/prisma/schema.prisma#L1359>)<br>ProductCollection / product_collections | merchantId  String   @map("merchant_id"); @@index([merchantId]) | id          String   @id @default(cuid()); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1373](<../../../../apps/api/prisma/schema.prisma#L1373>)<br>CollectionProduct / collection_products | Sem campo merchantId detectado; ownership pode vir por relação | id           String @id @default(cuid()); @@unique([collectionId, productId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1417](<../../../../apps/api/prisma/schema.prisma#L1417>)<br>Return / returns | merchantId String       @map("merchant_id"); @@index([merchantId, status]); @@index([merchantId, createdAt]) | id         String       @id @default(cuid()); @@index([merchantId, status]); @@index([merchantId, createdAt]); @@index([orderId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1439](<../../../../apps/api/prisma/schema.prisma#L1439>)<br>ReturnItem / return_items | Sem campo merchantId detectado; ownership pode vir por relação | id        String  @id @default(cuid()) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1451](<../../../../apps/api/prisma/schema.prisma#L1451>)<br>ReturnLabel / return_labels | Sem campo merchantId detectado; ownership pode vir por relação | id             String   @id @default(cuid()); returnId       String   @unique @map("return_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1465](<../../../../apps/api/prisma/schema.prisma#L1465>)<br>ReturnInspection / return_inspections | Sem campo merchantId detectado; ownership pode vir por relação | id            String        @id @default(cuid()); returnId      String        @unique @map("return_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1479](<../../../../apps/api/prisma/schema.prisma#L1479>)<br>ReturnRefund / return_refunds | Sem campo merchantId detectado; ownership pode vir por relação | id              String    @id @default(cuid()); returnId        String    @unique @map("return_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1495](<../../../../apps/api/prisma/schema.prisma#L1495>)<br>StoreMetricDaily / store_metrics_daily | merchantId         String   @map("merchant_id"); @@unique([merchantId, date]); @@index([merchantId, date]) | id                 String   @id @default(cuid()); @@unique([merchantId, date]); @@index([merchantId, date]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1512](<../../../../apps/api/prisma/schema.prisma#L1512>)<br>StoreProductMetric / store_product_metrics | merchantId  String   @map("merchant_id"); @@unique([merchantId, productId, month]); @@index([merchantId, month]) | id          String   @id @default(cuid()); @@unique([merchantId, productId, month]); @@index([merchantId, month]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1534](<../../../../apps/api/prisma/schema.prisma#L1534>)<br>ProductSearchVector / product_search_vectors | merchantId String   @map("merchant_id"); @@index([merchantId]) | id         String   @id @default(cuid()); productId  String   @unique @map("product_id"); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1558](<../../../../apps/api/prisma/schema.prisma#L1558>)<br>MerchantTeamMember / merchant_team_members | merchantId String       @map("merchant_id"); @@unique([merchantId, userId]); @@index([merchantId]) | id         String       @id @default(cuid()); @@unique([merchantId, userId]); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1573](<../../../../apps/api/prisma/schema.prisma#L1573>)<br>MerchantInvite / merchant_invites | merchantId String       @map("merchant_id"); @@index([merchantId]) | id         String       @id @default(cuid()); @@index([merchantId]); @@index([email, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1588](<../../../../apps/api/prisma/schema.prisma#L1588>)<br>MerchantDomain / merchant_domains | merchantId  String    @map("merchant_id"); @@index([merchantId]) | id          String    @id @default(cuid()); domain      String    @unique; @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1603](<../../../../apps/api/prisma/schema.prisma#L1603>)<br>StorefrontCart / storefront_carts | merchantId String   @map("merchant_id"); @@unique([merchantId, sessionId]) | id         String   @id @default(cuid()); @@unique([merchantId, sessionId]); @@index([expiresAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1622](<../../../../apps/api/prisma/schema.prisma#L1622>)<br>ProductPromotion / product_promotions | merchantId        String   @map("merchant_id"); @@index([merchantId, isActive, endsAt]) | id                String   @id @default(cuid()); @@index([merchantId, isActive, endsAt]); @@index([variantId, isActive]); @@index([categoryId, isActive]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1644](<../../../../apps/api/prisma/schema.prisma#L1644>)<br>StoryCategory / story_categories | merchantId String   @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, isArchived]) | id         String   @id @default(cuid()); @@index([merchantId, isArchived]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1661](<../../../../apps/api/prisma/schema.prisma#L1661>)<br>Story / stories | merchantId  String   @map("merchant_id"); @@index([merchantId]) | id          String   @id @default(cuid()); @@index([categoryId, isArchived]); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1680](<../../../../apps/api/prisma/schema.prisma#L1680>)<br>BudgetRequest / budget_requests | merchantId    String   @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, createdAt]); @@index([merchantId, status]) | id            String   @id @default(cuid()); @@index([merchantId, createdAt]); @@index([merchantId, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1703](<../../../../apps/api/prisma/schema.prisma#L1703>)<br>PromptExperiment / prompt_experiments | merchantId      String    @map("merchant_id"); @@unique([merchantId, name]); @@index([merchantId, status]); @@index([merchantId, createdAt]) | id              String    @id @default(cuid()); @@unique([merchantId, name]); @@index([merchantId, status]); @@index([merchantId, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1723](<../../../../apps/api/prisma/schema.prisma#L1723>)<br>PromptVariant / prompt_variants | Sem campo merchantId detectado; ownership pode vir por relação | id           String   @id @default(cuid()); @@unique([experimentId, name]); @@index([experimentId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1741](<../../../../apps/api/prisma/schema.prisma#L1741>)<br>PromptVariantResult / prompt_variant_results | Sem campo merchantId detectado; ownership pode vir por relação | id              String   @id @default(cuid()); @@unique([variantId, sessionId]); @@index([variantId, createdAt]); @@index([variantId, checkoutCompleted, createdAt], name: "idx_prompt_variant_results_funnel") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1773](<../../../../apps/api/prisma/schema.prisma#L1773>)<br>MarketplaceConfig / marketplace_configs | merchantId            String   @unique @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade) | id                    String   @id @default(cuid()); merchantId            String   @unique @map("merchant_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1791](<../../../../apps/api/prisma/schema.prisma#L1791>)<br>MarketplaceConnection / marketplace_connections | sellerMerchantId  String   @map("seller_merchant_id"); @@unique([buyerMerchantId, sellerMerchantId]) | id                String   @id @default(cuid()); @@unique([buyerMerchantId, sellerMerchantId]); @@index([buyerMerchantId, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1803](<../../../../apps/api/prisma/schema.prisma#L1803>)<br>FederatedProduct / federated_products | Sem campo merchantId detectado; ownership pode vir por relação | id                String   @id @default(cuid()); @@unique([sourceMerchantId, sourceProductId]); @@index([category]); @@index([sourceMerchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1824](<../../../../apps/api/prisma/schema.prisma#L1824>)<br>CrossStoreLineItem / cross_store_line_items | hostMerchantId      String   @map("host_merchant_id"); sellerMerchantId    String   @map("seller_merchant_id"); @@index([hostMerchantId]); @@index([sellerMerchantId]) | id                  String   @id @default(cuid()); @@index([hostMerchantId]); @@index([sellerMerchantId]); @@index([orderId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1847](<../../../../apps/api/prisma/schema.prisma#L1847>)<br>MarketplaceSettlement / marketplace_settlements | hostMerchantId        String   @map("host_merchant_id"); sellerMerchantId      String   @map("seller_merchant_id"); @@index([hostMerchantId]); @@index([sellerMerchantId]) | id                    String   @id @default(cuid()); @@index([hostMerchantId]); @@index([sellerMerchantId]); @@index([status]); @@index([transferScheduledAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1875](<../../../../apps/api/prisma/schema.prisma#L1875>)<br>MarketplaceSellerDebt / marketplace_seller_debts | sellerMerchantId          String   @map("seller_merchant_id"); @@index([sellerMerchantId]) | id                        String   @id @default(cuid()); @@index([sellerMerchantId]); @@index([status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1894](<../../../../apps/api/prisma/schema.prisma#L1894>)<br>RevenueManagerObservation / revenue_manager_observations | merchantId                String   @map("merchant_id"); @@index([merchantId, createdAt]); @@index([merchantId, observationWindowStart]) | id                        String   @id @default(cuid()); fingerprint               String   @unique; @@index([merchantId, createdAt]); @@index([merchantId, observationWindowStart]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1919](<../../../../apps/api/prisma/schema.prisma#L1919>)<br>RevenueManagerHypothesis / revenue_manager_hypotheses | merchantId                String   @map("merchant_id"); @@index([merchantId, status]); @@index([merchantId, createdAt]) | id                        String   @id @default(cuid()); @@index([merchantId, status]); @@index([merchantId, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1952](<../../../../apps/api/prisma/schema.prisma#L1952>)<br>RevenueManagerStrategyLesson / revenue_manager_strategy_lessons | merchantId                String   @map("merchant_id"); @@index([merchantId, recordedAt]) | id                        String   @id @default(cuid()); @@index([merchantId, recordedAt]); @@index([experimentId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1980](<../../../../apps/api/prisma/schema.prisma#L1980>)<br>ProtocolSession / protocol_sessions | merchantId                String   @map("merchant_id"); @@index([merchantId, expiresAt]); @@index([merchantId, currentState]) | id                        String   @id; @@index([merchantId, expiresAt]); @@index([merchantId, currentState]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:1997](<../../../../apps/api/prisma/schema.prisma#L1997>)<br>RecoveryAttempt / recovery_attempts | merchantId          String   @map("merchant_id"); @@index([merchantId, status]); @@index([merchantId, createdAt]) | id                  String   @id @default(cuid()); @@index([merchantId, status]); @@index([merchantId, createdAt]); @@index([sessionId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2018](<../../../../apps/api/prisma/schema.prisma#L2018>)<br>BuyerIntentMemoryConsent / buyer_intent_memory_consents | merchantId    String   @map("merchant_id"); @@id([merchantId, globalUserId]) | @@id([merchantId, globalUserId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2029](<../../../../apps/api/prisma/schema.prisma#L2029>)<br>CustomerIntentRecord / customer_intent_records | merchantId                String   @map("merchant_id"); @@index([merchantId, globalUserId]); @@index([merchantId, generatedAt]) | id                        String   @id @default(cuid()); @@index([merchantId, globalUserId]); @@index([merchantId, generatedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2047](<../../../../apps/api/prisma/schema.prisma#L2047>)<br>HoldoutGroupAssignment / holdout_group_assignments | merchantId      String   @map("merchant_id"); @@id([globalUserId, merchantId]); @@index([merchantId, cohort]) | @@id([globalUserId, merchantId]); @@index([merchantId, cohort]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2058](<../../../../apps/api/prisma/schema.prisma#L2058>)<br>AttributionTag / attribution_tags | merchantId                      String   @map("merchant_id"); @@unique([merchantId, orderId]); @@index([merchantId, cohort, createdAt]) | id                              String   @id @default(cuid()); @@unique([merchantId, orderId]); @@index([merchantId, cohort, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2082](<../../../../apps/api/prisma/schema.prisma#L2082>)<br>RevenueLiftSnapshot / revenue_lift_snapshots | merchantId      String   @map("merchant_id"); @@index([merchantId, calculatedAt]) | id              String   @id @default(cuid()); @@index([merchantId, calculatedAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2098](<../../../../apps/api/prisma/schema.prisma#L2098>)<br>NegotiationAttempt / negotiation_attempts | merchantId            String   @map("merchant_id"); @@index([merchantId, sessionId]) | id                    String   @id @default(cuid()); @@index([merchantId, sessionId]); @@index([cartFingerprint, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2116](<../../../../apps/api/prisma/schema.prisma#L2116>)<br>CartRecoveryStrategyPref / cart_recovery_strategy_prefs | merchantId String   @unique @map("merchant_id"); merchant Merchant @relation("CartRecoveryStrategyPref", fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId]) | id         String   @id @default(cuid()); merchantId String   @unique @map("merchant_id"); @@index([merchantId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2132](<../../../../apps/api/prisma/schema.prisma#L2132>)<br>BuyerGlobalProfile / buyer_global_profiles | Sem campo merchantId detectado; ownership pode vir por relação | globalUserId        String   @id @map("global_user_id") | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2148](<../../../../apps/api/prisma/schema.prisma#L2148>)<br>WhatsAppChannelConfig / whatsapp_channel_configs | merchantId      String   @unique @map("merchant_id"); merchant Merchant @relation(fields: [merchantId], references: [id], onDelete: Cascade) | id              String   @id @default(uuid()); merchantId      String   @unique @map("merchant_id"); @@index([whatsappNumber]); @@index([deviceId]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2179](<../../../../apps/api/prisma/schema.prisma#L2179>)<br>WhatsAppSession / whatsapp_sessions | merchantId        String   @map("merchant_id"); @@unique([merchantId, buyerPhone, deviceId]); @@index([merchantId, status]) | id                String   @id @default(uuid()); @@unique([merchantId, buyerPhone, deviceId]); @@index([merchantId, status]); @@index([lastActivityAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2199](<../../../../apps/api/prisma/schema.prisma#L2199>)<br>PaymentHold / payment_holds | merchantId       String    @map("merchant_id"); @@index([merchantId, status]) | id               String    @id @default(cuid()); paymentIntentId  String    @unique @map("payment_intent_id"); @@index([merchantId, status]); @@index([status, holdUntil]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2229](<../../../../apps/api/prisma/schema.prisma#L2229>)<br>InventoryLocation / inventory_locations | merchantId String   @map("merchant_id"); merchant Merchant          @relation("InventoryLocations", fields: [merchantId], references: [id], onDelete: Cascade); @@unique([merchantId, name]); @@index([merchantId, isActive]) | id         String   @id @default(cuid()); @@unique([merchantId, name]); @@index([merchantId, isActive]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2247](<../../../../apps/api/prisma/schema.prisma#L2247>)<br>InventoryItem / inventory_items | merchantId        String   @map("merchant_id"); merchant   Merchant          @relation("InventoryItems", fields: [merchantId], references: [id], onDelete: Cascade); @@unique([merchantId, sku, locationId]); @@index([merchantId, locationId]); @@index([merchantId, sku]) | id                String   @id @default(cuid()); @@unique([merchantId, sku, locationId]); @@index([merchantId, locationId]); @@index([merchantId, sku]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2274](<../../../../apps/api/prisma/schema.prisma#L2274>)<br>InventoryMovement / inventory_movements | merchantId  String                 @map("merchant_id"); merchant Merchant      @relation("InventoryMovements", fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, itemId, createdAt]); @@index([merchantId, kind, createdAt]) | id          String                 @id @default(cuid()); @@index([merchantId, itemId, createdAt]); @@index([merchantId, kind, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2294](<../../../../apps/api/prisma/schema.prisma#L2294>)<br>InventoryAlert / inventory_alerts | merchantId     String    @map("merchant_id"); merchant Merchant      @relation("InventoryAlerts", fields: [merchantId], references: [id], onDelete: Cascade); @@index([merchantId, acknowledged, createdAt]) | id             String    @id @default(cuid()); @@index([merchantId, acknowledged, createdAt]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2311](<../../../../apps/api/prisma/schema.prisma#L2311>)<br>ErpConnection / erp_connections | merchantId         String   @map("merchant_id"); merchant Merchant @relation("ErpConnections", fields: [merchantId], references: [id], onDelete: Cascade); @@unique([merchantId, provider]); @@index([merchantId, status]) | id                 String   @id @default(cuid()); @@unique([merchantId, provider]); @@index([merchantId, status]) | CREATE TABLE encontrado |
| [apps/api/prisma/schema.prisma:2333](<../../../../apps/api/prisma/schema.prisma#L2333>)<br>CrmConnection / crm_connections | merchantId         String    @map("merchant_id"); merchant Merchant @relation("CrmConnections", fields: [merchantId], references: [id], onDelete: Cascade); @@unique([merchantId, provider]); @@index([merchantId, status]) | id                 String    @id @default(cuid()); @@unique([merchantId, provider]); @@index([merchantId, status]) | CREATE TABLE encontrado |

## Validação operacional necessária

| Área | Código/evidência | Teste para liberar |
| --- | --- | --- |
| Pool/DB | [apps/api/src/shared/persistence/persistence.module.ts:1](<../../../../apps/api/src/shared/persistence/persistence.module.ts#L1>) | Medir conexões por réplica, pool timeout, transações longas, EXPLAIN ANALYZE e p95/p99 em dataset de staging. |
| Backup/restore | Nenhuma execução de restauração auditada | Definir RPO/RTO, restaurar cópia isolada e comparar constraints/contagens; registrar duração. |
| Readiness | [apps/api/src/shared/health/health.service.ts:21](<../../../../apps/api/src/shared/health/health.service.ts#L21>) | DB e Redis indisponíveis devem produzir readiness esperado; Redis ausente hoje permite ready. DB SELECT 1 não valida schema nem workers. |
| Redis/BullMQ | [API-040](<api/ADR-api-marketplace.md#api-040>); [ADR async](<ADR-ASYNC.md>) | Persistência/AOF, eviction, TLS, quotas, timeout e reconexão em staging. |
| Outbox/shutdown | [API-016](<api/ADR-api-shared.md#api-016>); [API-037](<api/ADR-api-shared.md#api-037>) | Crash/restart/SIGTERM, lease expirado, dead-letter e replay auditado. |
| Deploy | [API-038](<api/ADR-api-shared.md#api-038>); [API-045](<api/ADR-api-shared.md#api-045>) | Build congelado e rollout dos quatro apps; Vercel/Compose não foram implantados nesta auditoria. |
| Observabilidade | Logger/correlation/Prometheus e manifests em infra/observability | Correlacionar browser→API→outbox→worker→provedor; alertar oldest pending, duplicação, falha, latência e backlog. |
| Segredos | Arquivos reais .env não foram lidos | Validar existência/escopo/rotação com mecanismo que não exponha valores; secret scan do histórico ainda UNVERIFIED. |
| Custo/CPU/memória | Agents, JSON, uploads, timers, histórico de buyer | Medir event-loop lag/heap, limite de payload, timeout AI, tenant barulhento e concorrência de uploads. |

Teste proposto de carga: rampas de usuários simultâneos e volume de pedidos com mix de leitura/chat/checkout, incluindo 10.000 usuários como cenário a dimensionar, não promessa de capacidade. Definir antes SLO, hardware, tokens AI, pool, quotas de provider e limites por tenant. Todo resultado de throughput/performance permanece **REQUIRES LOAD VALIDATION**.
