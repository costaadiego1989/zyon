// Public Brazilian store URLs were already shared externally. Keep this
// locale-shaped path as an alias of the canonical storefront route.
export { default, generateMetadata } from "../../../store/[slug]/page";
