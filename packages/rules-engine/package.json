{
  "name": "@zyon/rules-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test dist/index.spec.js dist/__tests__/rules-engine.spec.js",
    "test:watch": "node --test --watch src/index.spec.ts"
  },
  "dependencies": {
    "@zyon/shared-types": "workspace:*"
  }
}
