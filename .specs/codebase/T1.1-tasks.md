# T1.1 — Type checkout use-case Prisma injections

**Goal:** remove `@Inject(PRISMA_CLIENT) prisma?: any` from start-checkout + send-chat use-cases. Replace with typed ports. Move billing-plan gate logic out of use-case into adapter. Behavior identical.

**Files touched:**
- start-checkout.use-case.ts (2 prisma blocks: billing gate, advanced rules)
- send-chat-message.use-case.ts (3 prisma blocks: advanced rules, promptExperiment, productVariant)

## Prisma concern → port map

| Concern | Query | New/reuse port | Home |
|---------|-------|----------------|------|
| Billing plan gate | merchantBillingSubscription.findUnique (status/trialEndsAt/stripePriceId) → BILLING_PLANS gates | NEW MerchantPlanPort.resolveExperienceFlags(merchantId) → {showBranding, voiceEnabled} | checkout/domain/ports |
| Advanced rules + policy | checkoutSetting.findUnique (advancedRules, interventionPolicy) | EXTEND CheckoutSettingsPort → getInterventionConfig(merchantId) → {advancedRules, interventionPolicy} | checkout/domain/ports |
| Prompt experiment | promptExperiment.findFirst (status=running, include variants) | NEW PromptExperimentPort.findRunningExperiment(merchantId) → {id, variants[]} \| undefined | checkout/domain/ports |
| Product variant lookup | productVariant.findFirst by sku+merchant (price, product, media) | NEW ProductVariantLookupPort.findBySku(merchantId, sku) → {name, price, imageUrl} \| undefined | checkout/domain/ports |

## Subtasks

### ST1 — MerchantPlanPort [billing gate]
- Define `checkout/domain/ports/merchant-plan.port.ts`: `MERCHANT_PLAN_PORT` symbol + interface `resolveExperienceFlags(merchantId): Promise<{ showBranding: boolean; voiceEnabled: boolean }>`
- Adapter `checkout/infrastructure/adapters/merchant-plan.adapter.ts`: inject payment `PAYMENT_PLATFORM_REPOSITORY` (getBilling) OR PRISMA_CLIENT scoped in infra; apply BILLING_PLANS + effectiveBillingPlan; return flags. Default showBranding=true, voiceEnabled=false on error (free-tier safe default — preserve existing catch).
- Wire in checkout.module.ts
- start-checkout: replace prisma block (72-90) with `await this.merchantPlan?.resolveExperienceFlags(input.merchant_id)`; keep @Optional
- Done: no prisma in that block; typecheck green; behavior identical (branding/voice flags)

### ST2 — Extend CheckoutSettingsPort [advanced rules]
- Add to `checkout/domain/ports/checkout-settings.port.ts`: `getInterventionConfig(merchantId): Promise<{ advancedRules: unknown[] | null; interventionPolicy: InterventionPolicy | null }>`
- Type InterventionPolicy properly (shared-types or checkout domain) — no `any`
- Adapter checkout-settings.adapter.ts: implement via checkout-settings use-case OR add read method
- Rewire both use-cases (start-checkout ~285, send-chat ~170)
- Done: advancedRules/policy typed; both use-cases use port

### ST3 — PromptExperimentPort [send-chat only]
- Define `checkout/domain/ports/prompt-experiment.port.ts`: `findRunningExperiment(merchantId): Promise<RunningExperiment | undefined>` where RunningExperiment = { id: string; variants: { id: string; weight: number; systemPrompt: string }[] }
- Adapter wraps experiments repo (reuse ExperimentRepositoryPort if it exposes findRunning; else new infra adapter)
- send-chat: replace block (243) — keep hash/weight selection logic in use-case (that's domain logic), only the fetch moves to port
- Done: typed experiment fetch; A/B selection unchanged

### ST4 — ProductVariantLookupPort [send-chat only]
- Define `checkout/domain/ports/product-variant-lookup.port.ts`: `findBySku(merchantId, sku): Promise<{ name?: string; price?: number; imageUrl?: string } | undefined>`
- Adapter in infra: prisma productVariant.findFirst (price/product/media), map to DTO
- send-chat: replace block (589)
- Done: typed variant lookup; tenancy (merchantId) preserved in where clause

## Invariants (MUST preserve)
- LLM never authorizes offers — send-chat message safety unchanged
- isSafeGeneratedMessage still enforced; deterministic fallback intact
- merchant_id in every where clause (tenancy)
- All ports @Optional in use-case (existing behavior — graceful when unwired)
- No behavior change — structure only

## Gate
```
cd apps/api && pnpm typecheck
cd apps/api && pnpm test -- start-checkout
cd apps/api && pnpm test -- send-chat
```
No `prisma?: any` remaining in either use-case constructor.

## Style
- No inline comments except where existing ones carry non-obvious domain intent (preserve those)
- JSDoc only on new port interfaces (public contract)
- Early return, no else; extracted methods SRP-named
