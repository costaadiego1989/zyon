# P0 Production Readiness Implementation\n\n**Requirement IDs:** P0-001 through P0-007  \n**Scope:** All 7 blocking issues for production deployment  \n**Complexity:** Large (7 parallel workstreams, minimal inter-dependencies)  \n**Target:** All P0s complete and verified this session  \n\n---\n\n## P0-001: Healthcheck Endpoint\n\n**What:** Add `/health` (liveness) and `/ready` (readiness) endpoints to NestJS API.  \n**Why:** Required for load balancers, K8s probes, uptime monitoring. Currently missing.  \n**Done When:**\n- `GET /health` returns 200 + `{status: \"ok\"}` instantly\n- `GET /ready` pings Prisma + returns 200 + `{status: \"ready\", db: \"connected\"}`\n- Both endpoints skip authentication\n- Endpoints tested with unit + e2e\n- `pnpm typecheck` passes\n\n**Tests:** Unit for service, e2e for endpoints  \n**Gate:** `pnpm test` + `pnpm test:prisma` pass  \n\n---\n\n## P0-002: Structured Logging\n\n**What:** Implement pino logger + nestjs-pino integration. Replace console.log in main.ts and shared modules.  \n**Why:** Production-grade JSON logging, correlation IDs for request tracing, required per Architecture Hardening decision.  \n**Done When:**\n- pino + nestjs-pino installed\n- `LoggerModule` exports singleton logger service\n- main.ts uses logger instead of console.log\n- `CorrelationIdMiddleware` injects `x-correlation-id` into AsyncLocalStorage\n- All external HTTP adapter calls log with correlation ID\n- Logs output JSON format\n- Tested: unit tests for middleware, integration test for log output\n- `pnpm typecheck` passes\n\n**Tests:** Unit for middleware, integration for pino config  \n**Gate:** `pnpm test` passes, manual log inspection confirms JSON output  \n\n---\n\n## P0-003: Dockerfile\n\n**What:** Multi-stage Dockerfile for API (dev + prod stages).  \n**Why:** Container deployment required. Currently missing.  \n**Done When:**\n- `Dockerfile` at repo root (or `apps/api/Dockerfile`)\n- Stage 1: builder (node + pnpm + build)\n- Stage 2: runner (minimal node, copy dist, ENV vars)\n- Runs `node dist/main.js` on port 3001\n- `.dockerignore` excludes node_modules, .git, coverage, .env\n- Tested: builds without errors, runs on localhost:3001, responds to `/health`\n- `pnpm typecheck` passes\n\n**Tests:** Manual docker build + docker run  \n**Gate:** `docker build -t aacp-api . && docker run -p 3001:3001 aacp-api` works  \n\n---\n\n## P0-004: CI/CD Pipeline\n\n**What:** GitHub Actions workflow: lint → typecheck → test → build → (deploy stub).  \n**Why:** Automated quality gates before merge. Currently no `.github/workflows/`.  \n**Done When:**\n- `.github/workflows/ci.yml` created\n- Triggers on push to all branches, PR\n- Steps: lint (eslint), typecheck (tsc), test (vitest + jest), build (nest + vite)\n- Fails fast on any step failure\n- Each step reports status in PR\n- Deploy step is stub (logs \"Ready to deploy\")\n- Tested: mock PR to verify workflow runs\n- `pnpm typecheck` passes\n\n**Tests:** Manual trigger via mock PR  \n**Gate:** Workflow completes successfully on main branch  \n\n---\n\n## P0-005: .env.example\n\n**What:** Create `apps/api/.env.example` with all required secrets + optional vars documented.  \n**Why:** Onboarding friction for new devs. Currently missing.  \n**Done When:**\n- `.env.example` lists:\n  - `OPENAI_API_KEY` / `DEEPSEEK_API_KEY` (required in prod)\n  - `DATABASE_URL` (required, e.g., PostgreSQL connection)\n  - `SHOPIFY_SHOP_DOMAIN`, `SHOPIFY_ADMIN_ACCESS_TOKEN`, `SHOPIFY_API_VERSION` (optional for Shopify sync)\n  - `ASAAS_API_KEY` (optional, crypto payments)\n  - `BREVO_API_KEY` (optional, email)\n  - `PORT` (optional, default 3001)\n  - `NODE_ENV` (optional, default development)\n- Comments explain each var\n- Matches vars in `assertRequiredSecretsInProduction`\n- No secrets committed\n- Tested: `.env.example` is readable, documentation clear\n\n**Tests:** Manual review  \n**Gate:** File exists, no secrets, all required vars documented  \n\n---\n\n## P0-006: Test rules-engine\n\n**What:** Write unit tests for `packages/rules-engine`. Safety-critical: discount authorization only source.  \n**Why:** 0% coverage. CLAUDE.md invariant: \"Discounts approved only by rules-engine.\"  \n**Done When:**\n- Unit tests cover: discount cap validation, margin enforcement, discount math\n- Tests: valid offer approved, overreach rejected, margin below threshold rejected\n- 100% of exported functions tested\n- All tests pass: `pnpm --filter @aacp/rules-engine test`\n- `pnpm typecheck` passes\n\n**Tests:** Jest/vitest unit tests  \n**Gate:** `pnpm --filter @aacp/rules-engine test` passes, coverage ≥ 80%  \n\n---\n\n## P0-007: Test decision-engine\n\n**What:** Write unit tests for `packages/decision-engine`. Critical: checkout orchestration.  \n**Why:** 0% coverage. Implements decision logic for checkout flow.  \n**Done When:**\n- Unit tests cover: decision logic paths, event routing, state transitions\n- Tests: happy path, edge cases (missing data, invalid state)\n- 100% of exported functions tested\n- All tests pass: `pnpm --filter @aacp/decision-engine test`\n- `pnpm typecheck` passes\n\n**Tests:** Jest/vitest unit tests  \n**Gate:** `pnpm --filter @aacp/decision-engine test` passes, coverage ≥ 80%  \n\n---\n\n## Dependencies & Sequencing\n\n- **P0-005 (.env.example)** has no dependencies — standalone\n- **P0-001 (healthcheck)** depends on Prisma (already present)\n- **P0-002 (logging)** has no hard dependencies\n- **P0-003 (Dockerfile)** depends on successful build (P0-004 validates)\n- **P0-004 (CI/CD)** depends on all code changes being complete\n- **P0-006 & P0-007 (tests)** have no external dependencies\n\n**Execution Plan:** Spawn 7 parallel subagents (one per P0). P0-001 through P0-003, P0-005, P0-006, P0-007 run immediately. P0-004 runs last (aggregates results).\n"}}